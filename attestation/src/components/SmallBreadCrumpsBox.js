import React from "react";

function SmallBreadCrumpsBox({ headerText }) {
    return (
        <section className="breadCrumpsBox contentCenter">
            <h1 className="breadCrumpsBox_heading">{headerText}</h1>
        </section>
    );
}

export default SmallBreadCrumpsBox;
